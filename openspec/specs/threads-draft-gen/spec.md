# Capability: Threads Draft Generation

## Purpose
Automatically generate natural social media drafts for Threads from top-performing Moltbook posts.

## Requirements

### Requirement: Generate Threads draft for qualifying posts

The system SHALL generate a Threads-optimized draft post using Ollama (with `think=True`) when a candidate post's `final_score` meets or exceeds the `REVIEW_MIN_SCORE` threshold from Settings. The threshold SHALL be configurable via the `REVIEW_MIN_SCORE` environment variable instead of being hardcoded. The draft SHALL be written in the language specified by `THREADS_LANGUAGE` (default: `en`). Since only posts meeting `REVIEW_MIN_SCORE` enter the review queue, all review items that are created SHALL have draft generation attempted (provided `source_url` is non-empty).

#### Scenario: Post meets configurable score threshold

- **WHEN** a review payload is built for a post whose `final_score` meets `REVIEW_MIN_SCORE` (read from Settings)
- **THEN** the system calls `ReviewPayloadService._generate_threads_draft()` with the raw content, top comments, score, and source URL, and stores the result in the `threads_draft` field of `ReviewItem`

#### Scenario: Post does not meet score threshold

- **WHEN** a post's `final_score` is below `REVIEW_MIN_SCORE`
- **THEN** the post is archived by `IngestionWorker` and no `ReviewItem` is created at all (no draft generation call is made)

#### Scenario: Draft generation fails

- **WHEN** Ollama returns an error or empty response during draft generation
- **THEN** the system logs a warning, sets `threads_draft` to an empty string, and continues without failing the review item creation

### Requirement: Draft uses natural conversational tone

The Threads draft prompt SHALL instruct the model to write in a natural, conversational tone suitable for social media, starting with a highly engaging, strictly single-sentence hook. The prompt MUST explicitly require the first short sentence to be emphasized with a unified format (e.g., solid brackets like 【 】) and prohibit bullet points, Markdown syntax (headings, bold, links), list-style formatting, and excessive emoji usage (max 3).

#### Scenario: Draft tone validation

- **WHEN** a Threads draft is generated by Ollama
- **THEN** the resulting text starts with an eye-catching bracket-enclosed hook that is strictly one short sentence (like 【 Hook 】) and reads as a natural social media post without bullet points, numbered lists, Markdown formatting, or more than 3 emoji

### Requirement: Draft appends original Moltbook link

The system SHALL programmatically append the original Moltbook post URL to the end of every generated Threads draft. The URL MUST NOT be included in the Ollama prompt output — it SHALL be appended after generation in the format `

{source_url}`.

#### Scenario: Draft includes source URL

- **WHEN** a Threads draft is successfully generated
- **THEN** the final stored `threads_draft` ends with a blank line followed by the post's `source_url` (e.g., `https://www.moltbook.com/posts/{id}`)

#### Scenario: Source URL is never generated by Ollama

- **WHEN** the Ollama prompt is constructed for draft generation
- **THEN** the prompt explicitly instructs the model NOT to include any URLs, and the source URL is appended programmatically after the model response is received

### Requirement: Draft generation uses think mode

The Ollama call for Threads draft generation SHALL use `think=True` (via `_chat_with_think_fallback`) to enable deeper reasoning, consistent with the pattern used in `ScoringService._score_with_ollama`.

#### Scenario: Think mode enabled for draft generation

- **WHEN** `_generate_threads_draft()` calls Ollama
- **THEN** the request payload includes `think: true` (or falls back to `thinking: true` per the compatibility pattern)

### Requirement: Draft language controlled by THREADS_LANGUAGE

The Threads draft SHALL be generated in the language specified by the `THREADS_LANGUAGE` setting. This setting is independent from `TRANSLATION_LANGUAGE`.

#### Scenario: THREADS_LANGUAGE set to English (default)

- **WHEN** `THREADS_LANGUAGE=en` (or unset, using default)
- **THEN** the generated Threads draft is written in English

#### Scenario: THREADS_LANGUAGE set to a non-English language

- **WHEN** `THREADS_LANGUAGE=zh-TW`
- **THEN** the generated Threads draft is written in Traditional Chinese (Taiwan)

### Requirement: Publish worker uses threads_draft

The publish worker SHALL use the `threads_draft` from the associated `ReviewItem` as the text sent to the Threads API. If `threads_draft` is empty or null, the system SHALL fall back to `candidate.raw_content`.

#### Scenario: Publish with threads_draft available

- **WHEN** a publish job runs for a candidate that has a non-empty `threads_draft` in its ReviewItem
- **THEN** the Threads API `publish_post` call uses the `threads_draft` text (which already includes the appended source URL)

#### Scenario: Publish with no threads_draft

- **WHEN** a publish job runs for a candidate whose ReviewItem has an empty `threads_draft`
- **THEN** the Threads API `publish_post` call falls back to `candidate.raw_content` with `source_url`
